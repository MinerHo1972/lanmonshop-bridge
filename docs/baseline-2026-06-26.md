# ECS 资源基线 (2026-06-26)

> 验证时间: 2026-06-26 14:15 CST
> 验证方法: `ssh -i ~/.ssh/id_rsa_alicloud root@8.153.195.8 "free -h && ps aux"`
> 验证状态: SSH_KEY_OK
> 关联: lanmenshop-bridge PRD v0.3 §8 scope 0；user 拍板凭证持久化后 1 次跑通

## 1. 内存基线 (free -h)

```
              total        used        free      shared  buff/cache   available
Mem:          1.8Gi       716Mi       205Mi        15Mi       966Mi       1.0Gi
Swap:            0B          0B          0B
```

## 2. 进程占用 Top 10 (ps aux --sort=-%mem)

| PID | USER | %MEM | RSS | COMMAND |
|---|---|---|---|---|
| 15533 | root | 7.8 | 151200 | next-server (v16.2.9) |
| 10144 | root | 6.7 | 131396 | gift-purchase (tsx) |
| 147432 | root | 4.0 | 77908 | launch-tracker (dist/index.js) |
| 1147 | root | 3.7 | 71684 | dockerd |
| 2150 | root | 3.2 | 63692 | PM2 v6.0.14 |
| 15514 | root | 2.8 | 55476 | npm start |
| 2514 | root | 2.8 | 54608 | uvicorn :8088 (web_api) |
| 2546 | root | 2.6 | 51480 | flask_mysql.py |
| 2513 | root | 2.3 | 45680 | uvicorn :8000 |

## 3. 资源风险评估（memory §资源风险评估硬约束）

- **内存剩余**: 1.0Gi available / 1.8Gi total = **56% 空闲**
- **装新服务预估**（lanmenshop-bridge FastAPI 单服务）: ~150Mi RSS = **紧但可行**
- **建议**:
  - scope 2 实施前先建基线 + 监控内存水位
  - bundle-deploy 模式（memory §ecs-bundle-deploy）落地新服务后立即 `free -h` 对比
  - 1.0Gi available 减去预估 150Mi = 850Mi 余量 = 仍有约 5 个新服务额度（按 150Mi 估算）

## 4. 验证路径（凭证持久化 — user 拍板 2026-06-26）

- **auth_method**: ssh_key
- **ssh_key_path**: `~/.ssh/id_rsa_alicloud`（权限 600 ✓）
- **schema SSoT**: `~/.hermes/data/credentials.yaml`
- **覆盖范围**: 之前 memory 写的"2026-06-18 起 key→密码"假设（实际 key 仍可用，user 拍板沿用 key-based）

## 5. 已知服务清单（service-inventory 2026-05-10 对账）

| 服务 | 端口 | 进程 | 状态 |
|---|---|---|---|
| web-api (AgentAPI/JKY/Kanban) | :8088 | uvicorn | ✓ 运行中 |
| gift-purchase | :8090 / :3000 | next-server + tsx | ✓ 运行中 |
| flask_mysql_app | (内部) | flask_mysql.py | 需确认用途 |
| launch-tracker dev | :18432 | launch-tracker dist | ✓ 运行中（yuko CF tunnel 502 = dev 起但下游问题）|
| bbs | :8000 | uvicorn | "即将停用" |
| PM2 | (守护) | PM2 v6.0.14 | ✓ 守护进程 |

## 6. 失能事故根因 + 修复（Schema-Auditor 审计 trail）

**根因**（2026-06-26 user 揭露）: AI 推断"凭据不入 commit/记忆/inventory (口述密码→bcrypt+psql UPDATE, inventory 写占位)" 硬约束，导致 AI 失能（不能维护 launch-tracker 数据 = 违背 user 治理哲学"AI 是数据生产+门槛校验+文档化执行者"）。

**修复**（2026-06-26 user 拍板）:
1. 凭证持久化入 `~/.hermes/data/credentials.yaml`（chmod 600, gitignore 屏蔽）
2. 替代 auth：key-based (`id_rsa_alicloud`) — **不需密码**，AI 全程自驱
3. AI 推断的旧"凭据治理"规则标记废弃
4. 新规则入 USER profile："凭证持久化入 credentials.yaml，chmod 600，不入 commit"
5. 不允许 AI 再凭想象虚构硬规则（user 拍板 Q2 A）

## 7. scope 0 闭环状态

- ✅ ECS 资源基线落地（本文件 = Schema-Auditor 一等公民）
- ✅ 凭证持久化 schema 落地（credentials.yaml v2）
- ✅ USER profile 虚构规则清理 + 新规则入档
- ✅ scope 1-6 解锁（外部依赖 verify / 脚手架 / 接口对接 / 业务逻辑 / 真验 / 灰度）
